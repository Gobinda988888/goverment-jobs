# Server Configuration
PORT=5000
NODE_ENV=development

# Database - Using Firebase Firestore (No separate MongoDB needed!)
# Firestore is part of Firebase - configured via FIREBASE_SERVICE_ACCOUNT below

# AI Service (Choose one)
# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-turbo-preview

# OR Google Gemini
GEMINI_API_KEY=your_gemini_api_key_here

# YouTube Data API
YOUTUBE_API_KEY=your_youtube_api_key_here

# Firebase Configuration
# For production: Add your Firebase service account JSON as a single line string
# Get it from: Firebase Console > Project Settings > Service Accounts > Generate New Private Key
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}

# JWT Authentication (for admin)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# Admin Credentials
ADMIN_EMAIL=admin@odishajobs.com
ADMIN_PASSWORD=ChangeThisPassword123!

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Scraper Configuration
SCRAPER_ENABLED=false
SCRAPER_CRON=0 */6 * * *
